# 남부지사 거래처 데이터 대조 및 문제점 보고서

본 문서는 **남부지사 계정 데이터**(`남부지사 거래처.xlsx` 및 `남부담당자.xlsx` 기준)와 **영일 본사 계정 데이터**(본사 ERP에서 추출한 데이터베이스 기준) 간의 거래처 정보를 상호 대조 분석하여 발견된 문제점과 이를 해결하기 위한 권장 사항을 정리한 보고서입니다. 지사 간 계정 정보 일원화 및 데이터 정확도 향상을 위한 기초 자료로 활용할 수 있습니다.

---

## 1. 대조 분석 결과 요약

| 대조 구분 | 건수 | 데이터 상태 및 내용 |
| :--- | :---: | :--- |
| **남부지사 계정 거래처 총계** | **757건** | 남부지사 계정 파일에 등록된 거래처 목록 |
| **영일 계정 내 남부지사 거래처 총계** | **269건** | 영일 본사 계정에서 '남부지사'로 분류된 거래처 목록 |
| **1) 양쪽 계정 코드 일치** | **23건** | 남부지사 계정과 영일 계정의 거래처코드가 완전히 일치하는 거래처 |
| **2) 코드 상이 / 상호 일치** | **267건** | 코드는 서로 다르지만 상호명이 일치하는 거래처 |
| **3) 남부지사 계정에만 존재 (영일 누락)** | **467건** | 영일 계정에 코드 및 상호명이 모두 존재하지 않는 거래처 |
| **4) 영일 계정에만 존재 (남부 누락)** | **137건** | 영일 계정에는 남부지사로 되어 있으나 남부지사 계정 목록에는 없는 거래처 |
| **세부 정보 불일치** | **51건** | 코드가 일치하는 거래처(23건) 중 상호명, 담당자, 신규일 등이 다른 항목 수 |

---

## 2. 주요 문제점 (Problems)

### 🔴 문제점 1: 거래처 코드 체계의 이원화 및 불일치
* **현상**: 
  * **남부지사 계정**에서는 거래처코드 접두사로 **`NB`**(예: `NB03122`)를 주로 사용하고 있습니다.
  * 그러나 **영일 계정**에는 `NB` 접두사로 시작하는 거래처코드가 **단 1건도 존재하지 않습니다**. 대신 영일 계정에서는 **`GJ`**(예: `GJ02143`) 접두사 코드로 등록되어 있습니다.
  * 상호명 대조 결과, 남부지사 계정의 `NB` 코드 거래처 중 **267건**이 영일 계정의 `GJ` 코드 거래처와 상호가 일치하는 동일 고객사임을 확인했습니다.
    * *예시: 남부지사 계정 `NB02001` ((주)기흥현대정비사업소, 담당자: 김복수) ↔ 영일 계정 `GJ02143` ((주)기흥현대정비사업소, 담당자: 김복수)*
* **영향**: 
  * 동일한 거래처임에도 양쪽 계정의 코드가 달라 거래 실적, 수금 현황 등의 데이터를 상호 대조하거나 통합 분석하기가 불가능합니다.
  * 데이터 연동 시 수작업 매핑이 지속적으로 발생하여 업무 비효율이 초래됩니다.

### 🔴 문제점 2: 영일 본사 계정 내 거래처 정보 누락 (467건)
* **현상**: 
  * 남부지사 계정에 등록된 757건의 거래처 중 **467건(약 61.7%)**은 영일 계정에 일치하는 코드나 상호명이 전혀 발견되지 않는 완전 누락 상태입니다.
* **영향**: 
  * 영일 계정에 해당 거래처 정보가 없으므로 해당 고객사에 대한 공식 매출 등록, 수금 입력, 세금계산서 발행 등의 본사 전산 처리가 불가능하거나 다른 임시 코드로 우회 처리되고 있을 가능성이 있습니다.

### 🔴 문제점 3: 영일 본사 계정 데이터의 동기화 미흡 (137건)
* **현상**: 
  * 영일 계정에서는 거래처그룹이 '남부지사'로 분류되어 있으나, 정작 남부지사 계정 목록에는 존재하지 않는 거래처가 **137건** 존재합니다.
* **영향**: 
  * 이미 폐업했거나 거래가 중단된 거래처 정보가 영일 계정에 여전히 '남부지사 활성 거래처'로 남아 있어 통계 왜곡을 야기할 수 있습니다. 
  * 혹은 지사 간 거래처 이관 처리가 영일 계정 상에 반영되지 않은 상태일 수 있습니다.

### 🟡 문제점 4: 코드 일치 거래처 간의 마스터 정보 불일치 (51건의 필드 오류)
* **현상**: 
  * 코드가 일치하는 23건의 거래처를 상세 비교한 결과, 상호명 불일치, 담당 영업사원 정보 불일치 등 총 51건의 세부 데이터 오류가 발견되었습니다.
    * *예시 1 (상호명 불일치): 남부지사 계정 `PR00024` ((주)대림오일) ↔ 영일 계정 `PR00024` (CHEMITUL(케미툴))로 상호명 상이*
    * *예시 2 (그룹 분류 불일치): 남부지사 계정 `PR00024` (거래처그룹1명: 남부지사) ↔ 영일 계정 `PR00024` (거래처그룹1명: 매입처)*
    * *예시 3 (담당자명 불일치): 남부지사 계정 `PR00024` (담당자명: 화성남부) ↔ 영일 계정 `PR00024` (담당 사원 등록 없음)*
* **영향**: 
  * 거래처 원장 조회 시 상호명 오기로 인한 혼선이 생길 수 있으며, 영업 담당자가 잘못 지정되어 실적 집계나 수금 관리에서 배당 오류가 발생할 수 있습니다.

### 🟡 문제점 5: 영일 본사 계정 필수 통계 관리 필드(업종분류코드, 지역코드) 누락
* **현상**: 
  * **영일 계정**에서는 업종 및 지역별 통계 관리를 위해 `업종분류코드`와 `지역코드`를 핵심 필드로 활용하고 있습니다.
  * 그러나 **남부지사 계정** 데이터에는 해당 필드들이 존재하지 않으며, 단지 텍스트 형식의 `채널` 또는 `주소1` 열만 포함되어 있습니다.
    * *예시: 영일 계정의 기존 거래처들은 `업종분류코드 = '120'`(경정비), `지역코드 = 0` 등으로 규격화되어 있으나, 남부지사 계정의 거래처들은 이러한 코드 데이터가 전혀 없음*
* **영향**: 
  * 남부지사 계정 데이터를 기준으로 영일 본사 계정에 신규 거래처를 일괄 등록할 때, 분석 대시보드에서 남부지사 거래처가 업종별/지역별 실적 집계에서 누락되거나 기타 분류로 표시되는 집계 왜곡이 발생합니다.

### 🔴 문제점 6: 담당자(사원) 코드 체계 불일치 및 데이터 충돌
* **현상**: 
  * **남부지사 계정**의 사원 리스트(`남부담당자.xlsx`)에서 정의한 사원 코드(`001`~`006`)가 **영일 계정**의 사원 코드(`사원_담당_코드`)와 완전히 다르게 지정되어 있으며, 데이터가 충돌하고 있습니다.
  * 동일한 인물임에도 코드가 다를 뿐만 아니라, 남부지사 계정의 사원 코드가 영일 계정 상에서는 전혀 다른 직원(본사/타지사 직원)의 코드로 중복 사용되고 있습니다.
    * *예시 1 (코드 상이 및 다른 직원 매핑)*:
      * 남부지사 계정 `001` (`김기상`) ↔ 영일 계정 `1` (`조성래`) | `김기상`은 영일 계정에서 `4`번 코드로 등록됨
      * 남부지사 계정 `002` (`김복수`) ↔ 영일 계정 `2` (`이완희`) | `김복수`는 영일 계정에서 `15`번 코드로 등록됨
      * 남부지사 계정 `003` (`김진철`) ↔ 영일 계정 `3` (`감우균`) | `김진철`은 영일 계정에서 `16`번 코드로 등록됨
      * 남부지사 계정 `004` (`장영식`) ↔ 영일 계정 `4` (`김기상`) | `장영식`은 영일 계정에서 `18`번 코드로 등록됨
      * 남부지사 계정 `005` (`박준성`) ↔ 영일 계정 `5` (`박문희`) | `박준성`은 영일 계정에서 `19`번 코드로 등록됨
    * *예시 2 (영일 계정 미등록 및 타 사원 코드 충돌)*:
      * 남부지사 계정 `006` (`화성남부`) ↔ 영일 계정 `6` (`이영광`) | 영일 계정 사원 마스터에 `화성남부`는 존재하지 않음
* **영향**: 
  * 만약 남부지사 계정의 거래처나 매출 실적 데이터를 영일 본사 계정에 동기화할 때 남부지사 계정의 사원 코드(`1`~`5`)를 그대로 맵핑할 경우, **영일 계정에서는 전혀 다른 사원(조성래, 이완희, 감우균 등)의 실적으로 저장되는 치명적인 데이터 왜곡**이 발생합니다.

---

## 3. 해결 방안 및 권장 사항 (Recommendations)

1. **거래처 코드 표준화 및 단일화 작업**
   * 남부지사 계정의 `NB` 코드와 영일 계정의 `GJ` 코드를 매핑하여 단일 코드로 통합해야 합니다. 향후 신규 등록 시에도 지사별 표준 접두사 규칙을 확립하여 영일 본사 계정의 체계를 따르도록 유도해야 합니다.
2. **영일 본사 계정에 누락 거래처 일괄 등록**
   * 영일 계정에 존재하지 않는 남부지사 계정 전용 거래처(467건)에 대해 실제 거래 여부 및 사업자 정보를 확인한 후 본사 데이터베이스에 신규 등록 절차를 진행해야 합니다.
3. **영일 본사 계정 내 휴면/이관 거래처 정리**
   * 영일 계정에만 남부지사로 분류되어 있는 137건의 거래처를 전수 조사하여 거래 종료 거래처는 '종료' 처리하고, 타 지사 이관 거래처는 그룹 정보를 업데이트해야 합니다.
4. **마스터 데이터 정기 동기화 프로세스 구축**
   * 상호명 및 담당 영업사원 정보의 불일치를 해결하기 위해 영일 본사 계정(ERP) 기준의 마스터 데이터를 남부지사 계정에 주기적으로 동기화하는 방안을 권장합니다.
5. **누락 필수 필드(업종분류 및 지역코드) 매핑 표준 수립**
   * 남부지사 계정의 `채널` 및 `주소1` 정보를 기반으로, 영일 본사 계정 등록 시 자동으로 `업종분류코드` 및 `지역코드`를 부여할 수 있는 변환 규칙(매핑 테이블)을 수립하여 통계 왜곡을 사전에 방지해야 합니다.
6. **사원(담당자) 코드 매핑 일원화**
   * 데이터 통합 및 동기화 작업 시 남부지사 계정의 사원 코드(`1`~`5`)를 사용하는 대신, 반드시 영일 본사 계정 기준 사원 코드(`4`, `15`, `16`, `18`, `19`)로 일치시킨 뒤 연동되도록 매핑 변환 프로세스를 도입해야 합니다.
